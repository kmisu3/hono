# Claude Code Instructions

Follow the repository-wide guidance in `AGENTS.md`.

Use the project skills when applicable:

- `/check`: run and report the standard project checks.
- `/commit [message]`: review, validate, stage, and commit the current task without pushing.
