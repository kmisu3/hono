services:
  api:
    build:
      context: .
      target: development
    ports:
      - "${API_PORT:-8787}:3000"
    environment:
      PORT: "3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules

volumes:
  node_modules:
